# Future multi-head roadmap (design only)

Potential future heads are candidate ranking, timing, intensity, NO_ACTION,
thread continuation, abort/replan, and outcome prediction, possibly with a
temporal context encoder. Each requires real session-level observations and
causal review; no heads or targets are implemented here.
