# V2 enhanced field collection plan

The first valid field session remains `cs_YEG6Z4FUQ6YX` and has the original
V2 telemetry profile. It is preserved as `V2_BASELINE_LEGACY_TELEMETRY`.

The new boot session `cs_NYF4WYCDLY6U` is empty before the next player and is
the first `V2_ENHANCED_TELEMETRY` collection session. It has numeric health
counters and per-decision diagnostics available for future sessions.

Field validation still requires three valid real sessions. Current progress is
one valid session, so two additional real sessions are required. The old V1
session is excluded and no schema profile is silently homogenized.
